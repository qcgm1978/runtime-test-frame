// Generated by CoffeeScript 1.6.3
(function() {
  'use strict';
  describe('Top 10 Movies for Programmers', function() {
    var temp;
    it('The Matrix', function() {
      var createWorld;
      return createWorld = function() {
        var arr, lightSpeed, start, _i, _results;
        start = {};
        arr = (function() {
          _results = [];
          for (var _i = 0; 0 <= Infinity ? _i <= Infinity : _i >= Infinity; 0 <= Infinity ? _i++ : _i--){ _results.push(_i); }
          return _results;
        }).apply(this);
        arr[-1] = 'God';
        arr[0] = start;
        return lightSpeed = 3 * 1e8;
      };
    });
    expect([1, 2, 3]).toEqual([1, 2, 3]);
    temp = [];
    temp[-1] = 'God';
    expect(temp[-1]).toEqual('God');
    expect(temp.length).toEqual(0);
    return expect(String(1e8).length - 1).toEqual(8);
  });

}).call(this);

/*
//@ sourceMappingURL=Top-10-Movies-for-Programmers.map
*/
